# mynode
This project includes a small Node script that prints an ASCII-style depiction of a chick eating rice.

Run it with:
```bash
node chick.js
```

You can also use npm:
```bash
npm start
```

Run tests with:
```bash
npm test
```
